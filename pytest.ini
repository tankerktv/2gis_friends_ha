[pytest]
testpaths = tests
asyncio_mode = auto

# Тесты умышленно не требуют Home Assistant: проверяется разбор протокола и
# построение рамки, то есть логика, которая ломается молча при изменениях на
# стороне 2ГИС. Как модули загружаются без HA — см. tests/conftest.py.
