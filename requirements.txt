websockets>=14,<16
paho-mqtt>=2.1,<3
httpx>=0.27,<0.29

# Только для TOKEN_PROVIDER=playwright (см. requirements-headless.txt):
#   pip install -r requirements-headless.txt && playwright install chromium
